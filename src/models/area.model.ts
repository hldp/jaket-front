export class Area {

    public coordinate: number[] = [];
    public radius: number;

    constructor(coordinate: number[], radius:number){
        this.coordinate = coordinate;
        this.radius = radius;
    }
}